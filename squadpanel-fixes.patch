diff --git a/index.html b/index.html
index 237390a..af0e49f 100644
--- a/index.html
+++ b/index.html
@@ -154,6 +154,7 @@ html,body{background:var(--bg);color:var(--text);font-family:'JetBrains Mono',mo
 .ftoggle.dead.active{color:var(--text-dim);background:#ffffff0d;}
 .ftoggle.empty.active{color:var(--text-dim);background:#ffffff0d;border-color:var(--text-dim);}
 .filter-sep{width:1px;background:var(--panel-edge);margin:2px 2px;}
+.obj-label{background:transparent!important;border:none!important;box-shadow:none!important;color:#fff;font-size:10px;font-weight:700;text-shadow:0 0 3px #000,0 0 3px #000;white-space:nowrap;pointer-events:none;}
 .map-ctrl-btn{
   width:28px;height:28px;display:flex;align-items:center;justify-content:center;
   border-radius:3px;border:1px solid var(--panel-edge);
@@ -804,8 +805,17 @@ function groupPlayersByVehicle(players) {
 
 // ─── NUEVA FUNCIÓN: CREAR MARCADORES PARA VEHÍCULOS ESTÁTICOS ──────────────
 function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
-  if (!vehicles || !corners) return;
-  if (!filters.empty) return; // Filtro de vehículos vacíos
+  if (!vehicles || !corners || !filters.empty) {
+    // Eliminar todos los marcadores estáticos si el filtro está desactivado
+    for (const id of Object.keys(markers)) {
+      if (id.startsWith('static_veh_')) {
+        map.removeLayer(markers[id]);
+        delete markers[id];
+      }
+    }
+    if (!vehicles || !corners || !filters.empty) return;
+  }
+  const seenStatic = new Set();
   for (const v of vehicles) {
     if (!v.position) continue;
     // Si ya está ocupado (su id aparece en seenVehicleIds), saltamos
@@ -816,6 +826,7 @@ function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
     if (!filters.veh) continue;  // mismo filtro que vehículos ocupados
 
     const key = `static_veh_${v.id}`;
+    seenStatic.add(key);
     const ll = gameToLatLng(v.position, corners);
     const group = {
       vehicleId: v.id,
@@ -844,6 +855,13 @@ function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
     m.on('click', () => selectStaticVehicle(group));
     if (!map.hasLayer(m)) map.addLayer(m);
   }
+  // Limpiar estáticos que ya no existen
+  for (const id of Object.keys(markers)) {
+    if (id.startsWith('static_veh_') && !seenStatic.has(id)) {
+      map.removeLayer(markers[id]);
+      delete markers[id];
+    }
+  }
 }
 
 // ─── SELECCIÓN DE VEHÍCULO ESTÁTICO ─────────────────────────────────────────
@@ -943,11 +961,9 @@ function updateMarkers(players, vehicles, corners) {
   // 4. Limpiar marcadores obsoletos
   for (const id of Object.keys(markers)) {
     if (!seen.has(id) && !id.startsWith('static_veh_')) {
-      // Para estáticos, ya se manejan en createStaticVehicleMarkers
       map.removeLayer(markers[id]);
       delete markers[id];
     }
-    // Los estáticos se limpian en createStaticVehicleMarkers si ya no existen
   }
 
   // 5. Follow selected
@@ -1013,7 +1029,7 @@ function updateObjectives(objectives, corners) {
   const seen = new Set();
   for (const o of objectives) {
     if (!o.position || !o.name) continue;
-    const key = `${o.name}_t${o.owningTeam}`;   // ← KEY ÚNICA
+    const key = o.name;   // clave por nombre (estable entre cambios de equipo)
     seen.add(key);
     const ll = gameToLatLng(o.position, corners);
     const icon = makeObjectiveIcon(o.owningTeam);
@@ -1021,7 +1037,7 @@ function updateObjectives(objectives, corners) {
       objectiveMarkers[key].setLatLng(ll).setIcon(icon);
     } else {
       const m = L.marker(ll, { icon, pane: 'objectivesPane' }).addTo(map);
-      m.bindTooltip(esc(o.name), { permanent: false, direction: 'top', offset: [0, -26] });
+      m.bindTooltip(esc(o.name), { permanent: true, direction: 'top', offset: [0, -30], className: 'obj-label' });
       objectiveMarkers[key] = m;
     }
   }
@@ -1520,6 +1536,16 @@ async function poll() {
     // ── Cargar mapa base ──
     const mapKey = normalizeMapName(data.layer || data.map);
     if (mapKey) {
+      const mapChanged = currentMapKey && mapKey !== currentMapKey;
+      if (mapChanged) {
+        // Limpiar todos los marcadores al cambiar de mapa
+        for (const id of Object.keys(markers)) { map.removeLayer(markers[id]); delete markers[id]; }
+        for (const id of Object.keys(objectiveMarkers)) { map.removeLayer(objectiveMarkers[id]); delete objectiveMarkers[id]; }
+        clearFobMarkers();
+        clearRallyMarkers();
+        clearAmmoMarkers();
+        lastSnapshot = null;
+      }
       loadBasemap(mapKey, !currentMapKey || mapKey !== currentMapKey || !imageLoaded);
     }
 
