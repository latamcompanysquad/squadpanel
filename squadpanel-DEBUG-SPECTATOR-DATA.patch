diff --git a/Squadpanelbroadcast.js b/Squadpanelbroadcast.js
index 8951e7b..76337f9 100644
--- a/Squadpanelbroadcast.js
+++ b/Squadpanelbroadcast.js
@@ -540,6 +540,10 @@ export default class SquadPanelBroadcast extends BasePlugin {
       if (withSpectating > 0) {
         const spectDeets = players.filter(p => p.isSpectating).map(p => `${p.name}(${p.steamID})`).join(', ');
         this.verbose(1, `📷 [SPECTATORS FOUND] ${spectDeets}`);
+        // Log detailed info
+        players.filter(p => p.isSpectating).forEach(p => {
+          this.verbose(2, `📷   → ${p.name}: position=${p.position ? 'YES' : 'NO'}, isAlive=${p.isAlive}, squad=${p.squadID ?? 'N/A'}, role=${p.role}`);
+        });
       }
     }
 
