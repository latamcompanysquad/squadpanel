diff --git a/index.html b/index.html
index 237390a..94483e6 100644
--- a/index.html
+++ b/index.html
@@ -154,6 +154,7 @@ html,body{background:var(--bg);color:var(--text);font-family:'JetBrains Mono',mo
 .ftoggle.dead.active{color:var(--text-dim);background:#ffffff0d;}
 .ftoggle.empty.active{color:var(--text-dim);background:#ffffff0d;border-color:var(--text-dim);}
 .filter-sep{width:1px;background:var(--panel-edge);margin:2px 2px;}
+.obj-label{background:transparent!important;border:none!important;box-shadow:none!important;color:#fff;font-size:10px;font-weight:700;text-shadow:0 0 3px #000,0 0 3px #000;white-space:nowrap;pointer-events:none;}
 .map-ctrl-btn{
   width:28px;height:28px;display:flex;align-items:center;justify-content:center;
   border-radius:3px;border:1px solid var(--panel-edge);
@@ -232,6 +233,8 @@ html,body{background:var(--bg);color:var(--text);font-family:'JetBrains Mono',mo
           <div class="ftoggle empty active" id="fempty" onclick="toggleFilter('empty')" title="Vehículos vacíos">🔄 VACÍOS</div>
           <div class="filter-sep"></div>
           <div class="ftoggle dead" id="fdead" onclick="toggleFilter('dead')" title="Mostrar muertos">💀 MUERTOS</div>
+          <div class="filter-sep"></div>
+          <div class="ftoggle flags active" id="fflags" onclick="toggleFilter('flags')" title="Banderas/Objetivos">🏳 FLAGS</div>
         </div>
       </div>
     </div>
@@ -637,7 +640,8 @@ function makeIcon(p, selected) {
 }
 
 function objectiveColor(owningTeam) {
-  return owningTeam === 1 ? '#4fa6e8' : owningTeam === 2 ? '#e0584f' : '#9aa092';
+  const t = parseInt(owningTeam, 10);
+  return t === 1 ? '#4fa6e8' : t === 2 ? '#e0584f' : '#9aa092';
 }
 function makeObjectiveIcon(owningTeam) {
   const color = objectiveColor(owningTeam);
@@ -650,7 +654,7 @@ function makeObjectiveIcon(owningTeam) {
 }
 
 // ─── FILTROS Y FOLLOW ────────────────────────────────────────────────────────
-const filters = { t1: true, t2: true, veh: true, dead: false, empty: true };
+const filters = { t1: true, t2: true, veh: true, dead: false, empty: true, flags: true };
 let lastSnapshot = null;
 let followSelected = false;
 
@@ -661,6 +665,7 @@ function toggleFilter(key) {
     const { players, vehicles, corners } = lastSnapshot;
     updateMarkers(players, vehicles, corners);
     updatePlayerList(players);
+    if (lastSnapshot.objectives) updateObjectives(lastSnapshot.objectives, corners);
   }
 }
 
@@ -804,8 +809,17 @@ function groupPlayersByVehicle(players) {
 
 // ─── NUEVA FUNCIÓN: CREAR MARCADORES PARA VEHÍCULOS ESTÁTICOS ──────────────
 function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
-  if (!vehicles || !corners) return;
-  if (!filters.empty) return; // Filtro de vehículos vacíos
+  if (!vehicles || !corners || !filters.empty) {
+    // Eliminar todos los marcadores estáticos si el filtro está desactivado
+    for (const id of Object.keys(markers)) {
+      if (id.startsWith('static_veh_')) {
+        map.removeLayer(markers[id]);
+        delete markers[id];
+      }
+    }
+    if (!vehicles || !corners || !filters.empty) return;
+  }
+  const seenStatic = new Set();
   for (const v of vehicles) {
     if (!v.position) continue;
     // Si ya está ocupado (su id aparece en seenVehicleIds), saltamos
@@ -816,6 +830,7 @@ function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
     if (!filters.veh) continue;  // mismo filtro que vehículos ocupados
 
     const key = `static_veh_${v.id}`;
+    seenStatic.add(key);
     const ll = gameToLatLng(v.position, corners);
     const group = {
       vehicleId: v.id,
@@ -844,6 +859,13 @@ function createStaticVehicleMarkers(vehicles, corners, seenVehicleIds) {
     m.on('click', () => selectStaticVehicle(group));
     if (!map.hasLayer(m)) map.addLayer(m);
   }
+  // Limpiar estáticos que ya no existen
+  for (const id of Object.keys(markers)) {
+    if (id.startsWith('static_veh_') && !seenStatic.has(id)) {
+      map.removeLayer(markers[id]);
+      delete markers[id];
+    }
+  }
 }
 
 // ─── SELECCIÓN DE VEHÍCULO ESTÁTICO ─────────────────────────────────────────
@@ -943,11 +965,9 @@ function updateMarkers(players, vehicles, corners) {
   // 4. Limpiar marcadores obsoletos
   for (const id of Object.keys(markers)) {
     if (!seen.has(id) && !id.startsWith('static_veh_')) {
-      // Para estáticos, ya se manejan en createStaticVehicleMarkers
       map.removeLayer(markers[id]);
       delete markers[id];
     }
-    // Los estáticos se limpian en createStaticVehicleMarkers si ya no existen
   }
 
   // 5. Follow selected
@@ -1008,12 +1028,47 @@ function tickTimer() {
 // ─── OBJECTIVES ──────────────────────────────────────────────────────────────
 const objectiveMarkers = {};
 
+let objectiveLine = null;
+
 function updateObjectives(objectives, corners) {
   ensureObjectivesPane();
   const seen = new Set();
+
+  // Dibujar/actualizar línea punteada entre banderas
+  const linePoints = objectives
+    .filter(o => o.position)
+    .map(o => gameToLatLng(o.position, corners));
+
+  if (!filters.flags) {
+    // Ocultar línea y marcadores
+    if (objectiveLine) { map.removeLayer(objectiveLine); objectiveLine = null; }
+    for (const key of Object.keys(objectiveMarkers)) {
+      map.removeLayer(objectiveMarkers[key]);
+      delete objectiveMarkers[key];
+    }
+    return;
+  }
+
+  if (linePoints.length >= 2) {
+    if (objectiveLine) {
+      objectiveLine.setLatLngs(linePoints);
+    } else {
+      objectiveLine = L.polyline(linePoints, {
+        color: '#ffffff',
+        weight: 1.5,
+        opacity: 0.7,
+        dashArray: '6, 6',
+        pane: 'objectivesPane',
+      }).addTo(map);
+    }
+  } else if (objectiveLine) {
+    map.removeLayer(objectiveLine);
+    objectiveLine = null;
+  }
+
   for (const o of objectives) {
     if (!o.position || !o.name) continue;
-    const key = `${o.name}_t${o.owningTeam}`;   // ← KEY ÚNICA
+    const key = o.name;
     seen.add(key);
     const ll = gameToLatLng(o.position, corners);
     const icon = makeObjectiveIcon(o.owningTeam);
@@ -1021,7 +1076,7 @@ function updateObjectives(objectives, corners) {
       objectiveMarkers[key].setLatLng(ll).setIcon(icon);
     } else {
       const m = L.marker(ll, { icon, pane: 'objectivesPane' }).addTo(map);
-      m.bindTooltip(esc(o.name), { permanent: false, direction: 'top', offset: [0, -26] });
+      m.bindTooltip(esc(o.name), { permanent: true, direction: 'top', offset: [0, -30], className: 'obj-label' });
       objectiveMarkers[key] = m;
     }
   }
@@ -1520,6 +1575,17 @@ async function poll() {
     // ── Cargar mapa base ──
     const mapKey = normalizeMapName(data.layer || data.map);
     if (mapKey) {
+      const mapChanged = currentMapKey && mapKey !== currentMapKey;
+      if (mapChanged) {
+        // Limpiar todos los marcadores al cambiar de mapa
+        for (const id of Object.keys(markers)) { map.removeLayer(markers[id]); delete markers[id]; }
+        for (const id of Object.keys(objectiveMarkers)) { map.removeLayer(objectiveMarkers[id]); delete objectiveMarkers[id]; }
+        if (objectiveLine) { map.removeLayer(objectiveLine); objectiveLine = null; }
+        clearFobMarkers();
+        clearRallyMarkers();
+        clearAmmoMarkers();
+        lastSnapshot = null;
+      }
       loadBasemap(mapKey, !currentMapKey || mapKey !== currentMapKey || !imageLoaded);
     }
 
@@ -1533,7 +1599,7 @@ async function poll() {
 
     // ── Actualizar marcadores de jugadores, vehículos ocupados y estáticos ──
     if (corners && data.players && data.players.length > 0) {
-      lastSnapshot = { players: data.players, vehicles: data.vehicles, corners };
+      lastSnapshot = { players: data.players, vehicles: data.vehicles, corners, objectives: data.objectives };
       updateMarkers(data.players, data.vehicles, corners);
     } else if (!data.players || data.players.length === 0) {
       lastSnapshot = null;
@@ -1552,6 +1618,7 @@ async function poll() {
         map.removeLayer(objectiveMarkers[name]);
         delete objectiveMarkers[name];
       }
+      if (objectiveLine) { map.removeLayer(objectiveLine); objectiveLine = null; }
     }
 
     // ── FOBs ──
