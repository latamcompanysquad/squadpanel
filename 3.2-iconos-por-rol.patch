diff --git a/index.html b/index.html
index 5ede22b..15194c7 100644
--- a/index.html
+++ b/index.html
@@ -364,59 +364,82 @@ function factionFromRole(role) {
   return KNOWN_FACTIONS.includes(prefix) ? prefix : null;
 }
 
-// ── ROL (categoría) ─────────────────────────────────────────────────────────────
-function roleCategory(p) {
-  if (p.isLeader) return 'leader';
+// ── ROL → ÍCONO WEBP ─────────────────────────────────────────────────────────
+// Extrae el "kit" del string BP_Soldier_<FACTION>_<Kit>_<NN> y lo mapea
+// al nombre del archivo T_role_<name>.webp disponible.
+function roleIconName(p) {
+  // Squad Leader: isLeader tiene prioridad absoluta, pero también chequeamos
+  // variantes SL_Crewman / SL_Pilot para elegir ícono más específico.
   const r = (p.role || '').toLowerCase();
-  if (r.includes('medic')) return 'medic';
-  if (r.includes('engineer') || r.includes('sapper')) return 'engineer';
-  if (r.includes('officer')) return 'officer';
-  return 'infantry';
-}
-
-function roleOverlay(category) {
-  switch (category) {
-    case 'leader':   // estrella
-      return `<path d="M14 7.3 L15.8 11.5 L20.3 11.9 L16.9 14.8 L17.9 19.2 L14 16.9 L10.1 19.2 L11.1 14.8 L7.7 11.9 L12.2 11.5 Z" fill="#fff"/>`;
-    case 'medic':    // cruz
-      return `<rect x="12.4" y="9" width="3.2" height="10" fill="#fff"/><rect x="9" y="12.4" width="10" height="3.2" fill="#fff"/>`;
-    case 'engineer': // tuerca
-      return `<path d="M18,14 L16,10.5 L12,10.5 L10,14 L12,17.5 L16,17.5 Z" fill="none" stroke="#fff" stroke-width="1.6"/><circle cx="14" cy="14" r="1.4" fill="#fff"/>`;
-    case 'officer':  // chevron
-      return `<path d="M9.5 16.2 L14 11.8 L18.5 16.2" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>`;
-    default:         // infantería: cruz fina (sin cambios visuales respecto a antes)
-      return `<line x1="14" y1="7" x2="14" y2="21" stroke="#000" stroke-width="1.3"/><line x1="7" y1="14" x2="21" y2="14" stroke="#000" stroke-width="1.3"/>`;
+  if (p.isLeader) {
+    if (r.includes('sl_crewman') || r.includes('slcrewman'))  return 'crewman_squadleader';
+    if (r.includes('sl_pilot')   || r.includes('slpilot'))    return 'pilot_squadleader';
+    return 'squadleader';
   }
-}
-
-// ── ICONOS SVG ────────────────────────────────────────────────────────────────
+  // Mapeo keyword → nombre de ícono (orden de prioridad: más específico primero)
+  if (r.includes('automaticrifleman_optic')) return 'automaticrifleman_optic';
+  if (r.includes('automaticrifleman') || r.includes('autorifleman') || r.includes('automatic_rifleman')) return 'automaticrifleman';
+  if (r.includes('crewman'))            return 'crewman';
+  if (r.includes('designatedmarksman') || r.includes('marksman')) return 'designatedmarksman';
+  if (r.includes('heavyantitank') || r.includes('heavy_at') || r.includes('hat')) return 'heavyantitank';
+  if (r.includes('lightantitank')  || r.includes('light_at') || r.includes('lat')) return 'lightantitank';
+  if (r.includes('machinegunner') || r.includes('machinegunner')) return 'machinegunner';
+  if (r.includes('medic'))              return 'medic';
+  if (r.includes('pilot'))              return 'pilot';
+  if (r.includes('raider'))             return 'raider';
+  if (r.includes('recruit') || r.includes('plain') || r.includes('unarmed')) return 'recruit';
+  if (r.includes('rifleman_scoped') || r.includes('rifleman2') || r.includes('rifleman_02') || r.includes('rifleman_03')) return 'rifleman_scoped';
+  if (r.includes('rifleman'))           return 'rifleman';
+  if (r.includes('sapper'))             return 'sapper';
+  if (r.includes('scout'))              return 'scout';
+  if (r.includes('sniper'))             return 'sniper';
+  if (r.includes('grenadier'))          return 'grenadier';
+  if (r.includes('engineer'))           return 'engineer';
+  return 'rifleman'; // fallback genérico
+}
+
+// ── ICONOS ────────────────────────────────────────────────────────────────────
 function makeIcon(p, selected) {
-  const color = p.teamID === 1 ? '#4fa6e8' : '#e0584f';
-  const category = roleCategory(p);
-  const ring = selected ? `<circle cx="14" cy="14" r="11" fill="none" stroke="#d9a441" stroke-width="2"/>` : '';
+  const color     = p.teamID === 1 ? '#4fa6e8' : '#e0584f';
+  const iconName  = roleIconName(p);
+  const iconSrc   = `./resources/T_role_${iconName}.webp`;
+  const ringStyle = selected
+    ? `box-shadow:0 0 0 2px #d9a441;border-radius:50%;`
+    : '';
+
+  // Círculo de color de equipo como fondo
+  const bgCircle = `<div style="
+    position:absolute;inset:0;border-radius:50%;
+    background:${color};opacity:.92;
+    border:1px solid #000;
+  "></div>`;
+
+  // Ícono de rol (webp blanco) centrado sobre el círculo
+  const roleImg = `<img src="${iconSrc}" alt="${iconName}"
+    style="position:absolute;inset:2px;width:24px;height:24px;
+           image-rendering:auto;filter:drop-shadow(0 0 1px #000a);"/>`;
+
+  // Badge de vehículo (esquina superior derecha)
   const vehicleBadge = p.vehicle
-    ? `<g transform="translate(17,1) scale(0.45)">
-         <rect x="3" y="7" width="16" height="10" rx="2" fill="#d9a441"/>
-         <rect x="7" y="4" width="8" height="5" rx="1" fill="#d9a441"/>
-         <circle cx="6" cy="18" r="2.5" fill="#000"/>
-         <circle cx="16" cy="18" r="2.5" fill="#000"/>
-       </g>`
+    ? `<div style="
+        position:absolute;top:-2px;right:-2px;
+        width:10px;height:10px;border-radius:2px;
+        background:#d9a441;border:1px solid #000;
+        display:flex;align-items:center;justify-content:center;font-size:6px;">🚗</div>`
     : '';
-  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 28 28">
-      <circle cx="14" cy="14" r="8" fill="${color}" opacity=".92" stroke="#000" stroke-width="1"/>
-      ${roleOverlay(category)}
-      ${vehicleBadge}
-      ${ring}
-     </svg>`;
 
+  // Badge de facción (esquina inferior derecha)
   const faction = factionFromRole(p.role);
   const factionBadge = faction
     ? `<img src="./resources/public__img__flags__circles__${faction}.webp" alt=""
-         style="position:absolute;right:-1px;bottom:-1px;width:12px;height:12px;border-radius:50%;
-                border:1px solid #000;background:#0b0f0c;" />`
+         style="position:absolute;right:-1px;bottom:-1px;width:12px;height:12px;
+                border-radius:50%;border:1px solid #000;background:#0b0f0c;"/>`
     : '';
 
-  const html = `<div style="position:relative;width:28px;height:28px;">${svg}${factionBadge}</div>`;
+  const html = `<div style="position:relative;width:28px;height:28px;${ringStyle}">
+    ${bgCircle}${roleImg}${vehicleBadge}${factionBadge}
+  </div>`;
+
   return L.divIcon({ html, iconSize:[28,28], iconAnchor:[14,14], className:'' });
 }
 
