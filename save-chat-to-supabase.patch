--- a/script.js
+++ b/script.js
@@ -1472,6 +1472,7 @@
 
     // ── Lista de jugadores en el panel ──
     updatePlayerList(data.players ?? []);
     updateChatMessages(data.chatMessages ?? []);
+    saveChatToSupabase(data.chatMessages ?? [], data.serverName ?? 'unknown', data.map ?? data.layer ?? 'unknown');
 
   } catch (e) {
     console.error('Error en poll:', e);