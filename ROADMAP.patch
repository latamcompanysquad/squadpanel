--- a/ROADMAP.md
+++ b/ROADMAP.md
@@ -6,7 +6,7 @@
 
 ---
 
-### FASE 1: Corrección de bugs & validación básica ✅ COMPLETADA
+### FASE 1: Corrección de bugs & validación básica ✅ COMPLETADA
 
 **Objetivo:** Asegurar que el mapa renderiza posiciones correctas
 
@@ -28,37 +28,52 @@ Crear un **minimapa táctico en tiempo real** sincronizado con servidor Squad, q
 
 ---
 
-### FASE 2: Sincronización Live & Backend 🎯 SIGUIENTE
+### FASE 2: Sincronización Live & Backend ✅ COMPLETADA
 
-**Objetivo:** Pasar de snapshots estáticos a actualizaciones en vivo desde servidor Squad
+**Completada.** Pipeline unidireccional Plugin → Worker → Frontend operativo.
 
-#### 2.1 - Configuración Backend
+#### 2.1 - Configuración Backend ✅
 
-- [ ] Crear estructura `backend/` (Node.js + Express)
-- [ ] Setup de SquadJS integration
-  - [ ] Escuchar evento `currentMatchData` (cada ~100ms)
-  - [ ] Parsear posiciones de jugadores
-  - [ ] Detectar cambios (join/leave/move/vehicle swap/death)
-- [ ] Extraer coordenadas de todos los 25 mapas
-  - [ ] Crear `backend/map-config.json` con cornerZero/One para cada mapa
-  - [ ] Validar contra CurrentMatchData.json real
+- [x] Plugin SquadPanelBroadcast operativo (SquadJS)
+  - [x] Lee `currentMatchData` cada intervalMs (configurable)
+  - [x] Parsea posiciones, equipo, rol, vehículo, squad, tickets
+  - [x] POST al Cloudflare Worker cada intervalo
+- [x] 26 corners reales extraídos de SquadCalc y validados en producción
+  - [x] Hardcodeados en `index.html` como fallback + enviados por el plugin vía cornerZero/One
 
-#### 2.2 - API REST + WebSocket
+#### 2.2 - API REST ✅ (sin WebSocket, polling suficiente)
 
-- [ ] Endpoint `GET /api/match/current` → JSON actual del servidor
-- [ ] WebSocket `/ws/match` → broadcast de cambios
-- [ ] Heartbeat cada 100ms con posiciones actualizadas
+- [x] Cloudflare Worker desplegado (`squadpanel-worker.latamcompanysquad.workers.dev`)
+- [x] `GET /api/match` → snapshot JSON completo del servidor
+- [x] KV para persistencia del snapshot más reciente
+- [ ] WebSocket descartado en favor de polling cada 2s (latencia aceptable)
 
-#### 2.3 - Frontend Updates
+#### 2.3 - Frontend ✅
 
-- [ ] Conectar a WebSocket en `index.html`
-- [ ] Listener de eventos
-- [ ] Actualizar panel lateral en tiempo real
-- [ ] Mostrar estado de conexión (dot + texto)
+- [x] Polling cada 2s a `/api/match`
+- [x] Marcadores por equipo (azul/rojo) con iconos diferenciados (infantry/vehículo)
+- [x] HUD con mapa activo, tickets T1/T2 (barra proporcional), estado de conexión
+- [x] Panel lateral: jugador seleccionado + lista de jugadores vivos
+- [x] Normalización de nombre de mapa (26 mapas)
+- [x] Fallback a diccionario local si el plugin no envía corners
 
-#### 2.4 - Testing
+#### 2.4 - Admin Commands ✅ (agregado en esta sesión)
 
-- [ ] Mock data con 2-3 jugadores en movimiento
-- [ ] Prueba de latencia (ms de delay desde evento a visualización)
-- [ ] Prueba con dataset completo (64+ jugadores)
+- [x] Tab Admin en panel lateral
+- [x] Target selection con anillo dorado en marcador
+- [x] Warn / Kick / Ban / Switch Team / Force Respawn (por jugador)
+- [x] Broadcast global
+- [x] Set Next Map / End Match
+- [x] Pause / Unpause Match
+- [x] `POST /api/admin/command` en Worker (cola KV con TTL 60s)
+- [x] `GET /api/admin/pending` para plugin
+- [x] `DELETE /api/admin/done/:id` para confirmar ejecución
+- [x] `pollAdminCommands()` en plugin (mismo loop de broadcast)
+- [x] Log de comandos en panel + toast de feedback
 
 #### Deliverables
 
-- `backend/server.js` operativo
-- `backend/map-config.json` (25 mapas)
-- WebSocket funcionando en frontend
-- Jugadores actualizándose en tiempo real
+- [x] `index.html` con panel admin completo
+- [x] `worker-admin.js` (funciones a integrar en worker existente)
+- [x] `plugin-patch.js` (método a agregar a SquadPanelBroadcast)
+- [x] Jugadores actualizándose en tiempo real (polling 2s)
 
 ---
 
-### FASE 3: Iconografía Avanzada & UX Mejorada 🎨
+### FASE 3: Iconografía Avanzada & UX Mejorada 🎯 SIGUIENTE
 
 **Objetivo:** Diferenciar visualmente rol, equipo, vehículo; agregar interactividad
 
@@ -139,14 +154,14 @@ Para cada sesión, usar este formato:
 | Fase         | Duración Estimada  | Complejidad |
 | ------------ | ------------------- | ------------ |
 | 1 (Bugs)     | 1 sesión            | Baja ✅      |
-| 2 (Backend)  | 3-4 sesiones        | Media        |
+| 2 (Backend)  | 3 sesiones          | Media ✅     |
 | 3 (UX/Icons) | 2-3 sesiones        | Media        |
 | 4 (Replay)   | 4-5 sesiones        | Alta         |
-| **Total**    | **10-13 sesiones**  | —            |
+| **Total**    | **10-13 sesiones**  | 2/4 fases ✅ |
 
 ---
 
 ## ✅ Checklist de Validación
 
-### Fase 1 (Actual)
+### Fase 1 ✅
 
 - [x] Posición de jugador correcta en 4 esquinas
 - [x] Círculos de FOB renderean
 - [x] Rutas de imágenes confirmadas
-- [ ] **Siguiente:** Iniciar Phase 2 backend setup
+- [x] ~~Siguiente: Iniciar Phase 2 backend setup~~ → COMPLETADO
 
-### Fase 2 (Soon)
+### Fase 2 ✅
 
-- [ ] Backend server arrancando sin errores
-- [ ] SquadJS conectando a servidor
-- [ ] WebSocket broadcasting cambios
-- [ ] Frontend recibiendo y aplicando updates
-- [ ] Latencia <200ms observable
+- [x] Plugin SquadPanelBroadcast corriendo en producción
+- [x] Cloudflare Worker recibiendo y sirviendo datos
+- [x] Frontend polling y renderizando marcadores en tiempo real
+- [x] Latencia ~2s (polling), aceptable para uso táctico
+- [x] Admin commands: warn/kick/ban/broadcast/map/endmatch/pause
 
-### Fase 3 (Later)
+### Fase 3 (SIGUIENTE)
 
 - [ ] Iconos renderean correctamente
 - [ ] Hover tooltips funcionan
@@ -175,3 +190,16 @@ Para mantener este roadmap actualizado:
 4. Pasar tareas incompletas a siguiente sesión
 5. Mover tarea entre secciones (En Progreso → Completada)
 
-Esto permite a futuros agentes contextualizarse rápidamente sin perder progreso.
+Esto permite a futuros agentes contextualizarse rápidamente sin perder progreso.
+
+---
+
+## 📋 Sesión — Admin Commands
+
+### Tareas Completadas
+- [x] Panel Admin tab en frontend (warn, kick, ban, switch team, respawn, broadcast, set map, end match, pause/unpause)
+- [x] Target selection con anillo visual en marcador
+- [x] Worker: POST /api/admin/command, GET /api/admin/pending, DELETE /api/admin/done/:id
+- [x] Plugin patch: pollAdminCommands() + executeAdminCommand() dispatcher
+- [x] Log de comandos + toast de feedback en UI
+- [x] ROADMAP actualizado al estado real
+
+### Próxima Sesión
+- [ ] Fase 3: Spritesheet de iconos por rol (SL, medic, engineer, etc.)
+- [ ] Fase 3: Hover tooltip mejorado (nombre + rol + squad)
+- [ ] Fase 3: Filter panel (toggle por equipo/rol/vehículo)
+- [ ] Integrar worker-admin.js al worker de Cloudflare real
+- [ ] Probar ciclo completo: click botón → KV → plugin poll → RCON → Squad
